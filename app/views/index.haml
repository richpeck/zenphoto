- # Show Photos etc
%h2 Albums (#{@albums.count})
= form_tag "/" do
  - @albums.each do |album|
    = label_tag album.id, style: "display:block;" do
      = check_box_tag :albums, value: album.id, id: album.id
      = album.title
  = submit_tag
